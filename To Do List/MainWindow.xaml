<Window x:Class="To_Do_List.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:To_Do_List"
        mc:Ignorable="d"
        Title="Список дел"
        Height="460"
        Width="800"
        Background="#FFFDFAF1">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Grid.Row="0"
                   Text="Список дел"
                   FontSize="26"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

        <ListBox x:Name="listToDo"
                 Background="#FDF5DF"
                 BorderBrush="Gray"
                 BorderThickness="1"
                 Margin="5"
                 Grid.Column="0"
                 Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding Path = Name}" />
                        <LineBreak />
                        <Run Text="{Binding Path = Date, StringFormat = dd.MM.yyyy}" />
                        <LineBreak />
                        <Run FontSize="10px"
                             Text="{Binding Path = Description}" />
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <CheckBox x:Name="checkBoxNewTask"
                  Click="CheckBoxNewTask_Click"
                  IsChecked="False"
                  Grid.Column="1"
                  Grid.Row="0"
                  Margin="5"
                  VerticalAlignment="Bottom">Добавление новых дел</CheckBox>

        <GroupBox x:Name="groupBoxToDo"
                  Grid.Column="1"
                  Grid.Row="1"
                  Margin="5"
                  Background="#FDF5DF"
                  Visibility="Hidden">
            <StackPanel Margin="10">
                <TextBlock Margin="0,0,0,5">Название:</TextBlock>
                <TextBox x:Name="titleToDo" />

                <TextBlock Margin="0,10,0,5">Выполнить до:</TextBlock>
                <DatePicker x:Name="dateToDo" />

                <TextBlock Margin="0,10,0,5">Описание:</TextBlock>
                <TextBox x:Name="descriptionToDo"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto"
                         BorderBrush="Gray"
                         BorderThickness="1"
                         MinLines="10"
                         MaxLines="10"
                         MinHeight="161.6px" />
            </StackPanel>
        </GroupBox>

        <Button x:Name="buttonDelete"
                Click="ButtonDelete_Click"
                Content="Удалить дела"
                Padding="5"
                HorizontalAlignment="Center"
                Background="#5EBEC4"
                Foreground="Black"
                Margin="5"
                Grid.Column="0"
                Grid.Row="2" />

        <Button x:Name="buttonAdd"
                Click="ButtonAdd_Click"
                Content="Добавить дело"
                Padding="5"
                HorizontalAlignment="Center"
                Background="#5EBEC4"
                Foreground="Black"
                Margin="5"
                Grid.Column="2"
                Grid.Row="2">
            <Button.ToolTip>
                <ToolTip Content="При добавлении дело появится в списке слева, и его придется выполнить" />
            </Button.ToolTip>
        </Button>
    </Grid>
</Window>
